@model RadarLiterario.Models.Usuario

@{
    ViewData["Title"] = "Meus Dados - ";
}

<h3>Meus dados</h3>

<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Nome)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Nome)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Sobrenome)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Sobrenome)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.DataDeNascimento)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.DataDeNascimento)
        </dd>
    </dl>
</div>

<div class="align-text-bottom text-right">
    <a asp-action="Edit" asp-route-id="@Model.Email">
        <i class="bi bi-pencil-square"></i> Alterar dados e/ou senha
    </a>
</div>
<br />
<div>
    <h4>Segurança e privacidade</h4>
    <div class="row">
        <div class="p-3 col-sm-auto">
            <a class="btn btn-sm btn-dark btn-block" href="#">Alterar frase de segurança</a>
            <button type="button" class="btn btn-sm btn-danger btn-block" data-toggle="modal" data-target="#deleteModal">
                Deletar conta
            </button>
        </div>
    </div>
</div>

<!-- Modal -->
<div class="modal fade" id="deleteModal" tabindex="-1" role="dialog" aria-labelledby="modalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="modalLabel">Deletar</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <h6>Tem certeza de que quer ENCERRAR e DELETAR a sua conta permanentemente?</h6>
                <p>
                    Depois de deletada, a sua conta não poderá mais ser recuperada. Caso queira voltar a utilizar os serviços deste site,
                    deverá realizar um novo cadastro de usuário.
                </p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">
                    <i class="bi bi-x-octagon"></i> Cancelar
                </button> |
                <form asp-action="Delete">
                    <input type="hidden" asp-for="Email" />
                    <button type="submit" class="btn btn-danger">
                        <i class="bi bi-trash3"></i> Deletar
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>